Flask==3.1.3
GitPython==3.1.46
neo4j==6.1.0
chromadb==1.5.5
tree-sitter==0.25.2
tree-sitter-python==0.25.0
python-dotenv==1.2.2

# Transitive deps (pinned from the shipped venv) can be added if needed.
